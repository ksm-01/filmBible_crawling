<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.benew.filmbiblecrawling.mapper.PlayListHasVideoMapper">

    <select id="countPlayListHasVideo" resultType="Integer">
        select
            count(*)
        from FB_PLAYLIST_has_VIDEO
        where fb_phv_playlist_id = #{playListId} and fb_phv_video_id = #{videoId}
    </select>

    <insert id="savePlayListHasVideo">
        insert into FB_PLAYLIST_has_VIDEO (
            fb_phv_playlist_id,
            fb_phv_video_id
        ) values (
             #{playListId},
             #{videoId}
         )
    </insert>

</mapper>